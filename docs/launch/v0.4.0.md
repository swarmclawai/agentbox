# agentbox v0.4.0

## Headline

Agentbox now has a local run library and launch-ready demo scenarios.

## Release Notes

`@swarmclawai/agentbox@0.4.0` makes recorded runs easier to revisit after the first replay. You can list runs, open the latest replay, generate a searchable offline library page, compare two runs, and safely clean old artifacts.

The demo command now ships three deterministic scenarios. Each creates a self-contained replay, Markdown report, and redacted export zip:

```bash
agentbox demo --scenario success
agentbox demo --scenario failure
agentbox demo --scenario mcp-risk
```

New commands:

```bash
agentbox list
agentbox library --open
agentbox open latest
agentbox compare <base> <head>
agentbox clean --keep 10 --dry-run
```

Run targets now support `latest`, exact ids, unique id prefixes, run directories, and paths to files inside a run.

## Short Post

Agentbox v0.4.0 is out.

The new release adds a local run library for AI agent replays:

- `agentbox list`
- `agentbox library --open`
- `agentbox open latest`
- `agentbox compare <base> <head>`
- `agentbox clean --keep 10 --dry-run`

Still local-first. Still self-contained HTML replays. Now much nicer once you have more than one run.
